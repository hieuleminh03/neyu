server:
  port: 8081
spring:
  application:
    name: authentication-service
  profiles:
    active: @spring.profiles.active@

management:
  endpoints:
    web:
      exposure:
        include: '*'

logging:
  level:
    web: INFO
    root: info
